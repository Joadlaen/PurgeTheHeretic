using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//an initial idea was to add cover mechanics which included the red sprite.
//it would have similar effects to warhammer cover Terrain which block only vehicles 
//it would also affect armour saves by adding one to the roll.
//armour saves weren't implemented and the idea was discarded early in production.
public class CoverScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
